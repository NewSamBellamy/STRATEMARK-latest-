.firebase/
node_modules/
dist/
dist-electron/
release/
coverage/
playwright-report/
test-results/
storybook-static/
*.log
.DS_Store
.env
.env.*
.env.local
*.tsbuildinfo

# local visual-capture artifacts
apps/web/shots/

# AI coding-tool artifacts. `.aider.chat.history.md` is a verbatim transcript of
# an assistant session — it does not belong in a public repository, and the
# tag cache is a large binary that churns on every run.
.aider*
.claude/
.cursor/
.opencode/
*.session.json
